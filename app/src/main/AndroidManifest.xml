<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eijs.creditscore">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_star_rating"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_star_rating"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".java.DetailedHistory"></activity>
        <activity android:name=".java.HistoryDocList" />
        <activity android:name=".java.EntDocListActivity"
            android:label="DOCTOR LIST"/>
        <activity android:name=".java.LoginActivity" />
        <activity
            android:name=".java.SplashActivity"
            android:label="Spash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".java.MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>